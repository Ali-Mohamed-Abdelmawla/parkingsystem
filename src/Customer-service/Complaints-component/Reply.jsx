import React from 'react';
import styles from '../styles/Employees.module.css'

const Reply = ({ complaint, onClose }) => (
  <div>
  <textarea placeholder="write to the customer"></textarea>
  <button></button>
  </div>
);

export default Reply;